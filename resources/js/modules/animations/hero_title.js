export const hero_title = (gsap) => {
    // const title = gsap.utils.toArray(".title-animation span");
    // if (!title) return;
    // let tl = gsap.timeline({
    //     defaults: {
    //         duration: 0.2,
    //     },
    // });
    // title.forEach((item) => {
    //     item.addEventListener("mouseenter", function () {
    //         tl.to(item, {
    //             y: "-60rem",
    //         });
    //     });
    //     item.addEventListener("mouseleave", function () {
    //         tl.to(item, {
    //             y: 0,
    //         });
    //     });
    // });
};

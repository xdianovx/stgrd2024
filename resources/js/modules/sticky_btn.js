export const sticky_btn = (gsap) => {
    const btns = document.querySelectorAll(".square_btn");

    if (!btns) return;

    btns.forEach((item) => {
        item.addEventListener("mousemove", function () {});
    });
};
